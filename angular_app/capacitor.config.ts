import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vehicleq.app',
  appName: 'VehicleQ',
  webDir: 'dist/angular_app/browser'
};

export default config;
